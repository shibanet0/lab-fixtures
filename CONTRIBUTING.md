# Contributing

New fixtures must be original or clearly compatibly licensed. Do not include
real personal data. Use descriptive names and document unusual behaviour.

Never change an existing fixture's bytes. Add a `-v2` name instead, because
downstream projects content-address these files.
